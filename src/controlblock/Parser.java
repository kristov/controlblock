package controlblock;

public class Parser {
    /* Parses the string and returns the root id */
    public int parseString(String chunk) {
        for (int i = 0; i < chunk.length(); i++) {
            char c = chunk.charAt(i);
            if (c == ' ') {
                
            }
            if (c == '(') {
            
            }
            if (c == ')') {
                
            }
            if (c == 'a') {
                
            }
        }
        return 0;
    }
}
